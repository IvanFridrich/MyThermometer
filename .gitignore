.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Secrets — never commit the real file, only secrets.h.example
include/secrets.h

# OTA TLS cert+key — never commit the real file, only certs.h.example
include/certs.h

# LLVM coverage artefacts
*.profraw
*.profdata
coverage/

# Compiled test binaries
test_runner
test_runner*.exe
test_runner_*
# Local ASan/UBSan scratch builds + Windows debug symbols
san_*
*.pdb

# SonarQube scanner and analysis cache
sonar-scanner*
.sonar/
.scannerwork/
__pycache__/
